# Per-VM config. Copy to repo.env and edit. NOT committed (gitignored).
# Declares which repo lives on THIS vm.

REPO_DIR="$HOME/todo-factory-demo"   # path to the repo checkout on this VM
REPO_SLUG="petealbertson/todo-factory-demo"  # owner/repo on GitHub
